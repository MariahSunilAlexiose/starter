echo 'Styling, testing your project before committing...'

# Type check
yarn check-types || {
  echo '❌ Failed Type check...'
  exit 1
}

# Run lint-staged (only affects staged files)
npx lint-staged || {
  echo '❌ Lint-staged checks failed...'
  exit 1
}

echo '✅ All checks passed. Committing...'